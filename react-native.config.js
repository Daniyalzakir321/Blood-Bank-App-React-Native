module.exports = {
    project: {
      ios: {},
      android: {}, // grouped into "project"
    },
    assets: ["./assets/fonts"], // stays the same
  };
  
  // Run link to link fonts to both android and ios p
  // npx react-native link